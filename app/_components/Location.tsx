interface LocationProps {
    image_src: string;
    location: string;
}

const Location = ({image_src, location}: LocationProps) => {
  return (
    <div>Location</div>
  )
}

export default Location